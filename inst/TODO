
TODO list:

tester le package avec testthat

faire un report.modelinfo.lm et .lmer et .glmer

fonction pour crééer des jolies label à partir de nom de variables à chier (make.label)

rajouter des exemples avec transpose, drop.x1, drop.x2, y.label, report.modelinfo, remove.missing

rajouter une vignette avec des exemples pour chaque model

rajouter une vignette avec un exemple de rapport stat complet

créer une fonction remove.stat? pour  enlever 
une statistique à un report.quanti ou report.quali


add SEM and color in gg_desc_quanti if x1!=NULL and x2=NULL


create the transpose.desc function to put statistics in column

add the possibility to combine in column qualitative and quantitative stats
(example of xavier request)


Ajouter un exemple dans la vignette avec la table animal disposition de peridry


create a highlight argument in report.doc to underline some cells in the table

add a note in the vignette to say that some will be horrified to the fact that I use 
the sign = and not <- in my code. I never understood why to use 2 signs instead of 1
is better... I use R for a long time, I always coded like this and it never
causes me any issue. So because I'm antoconformist to some extends,
I'll continue coding like this unless someone give the agument of the century!



Ajouter l'option n_percent = T par défaut si F on affiche d'abord le pourcentage et après l'effectif


reporting des AE: 
          nécessite 2 modifications de la report.quali
          pouvoir transposer la table: stat en colonne et niveaux en ligne
          compter au niveaux des sujets ceux qui présentent at least one event
          le total des AE est compter sur le niveau le plus haut (SOC name) le PT name est une sous catégorie
          see Z:\D965\1808_NT2\STAT\RESULTS\Statistical Report\Stat report NT2
          
implement formula use?
formula=(y~x1+x2)
all.vars(formula)


